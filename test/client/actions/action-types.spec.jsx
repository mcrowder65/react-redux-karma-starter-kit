import {SET_USERNAME} from "../../../src/client/actions/action-types";

describe("src/client/actions/action-types.spec.jsx", () => {
    describe(`${SET_USERNAME}`, () => {
        it(`${SET_USERNAME} should equal ${SET_USERNAME}`, () => {
            expect(SET_USERNAME).eql(SET_USERNAME);
        });
    });
});